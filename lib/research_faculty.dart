import 'package:flutter/material.dart';

class FacultyResearch extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
          child: Text(
        ' insert any graph for demo of faculty research tab',
        style: TextStyle(fontSize: 32.0),
      )),
    );
  }
}
