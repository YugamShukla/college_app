import 'package:flutter/material.dart';

class FacultyAwards extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(
          'Insert any graph for demo of awards tab',
          style: TextStyle(fontSize: 35.0),
        ),
      ),
    );
  }
}
